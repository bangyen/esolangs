`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

#`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

M`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

z`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

M`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

X`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

D`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

z`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

I`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

z`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

#`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

#`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

z`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

F`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

j`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

B`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

F`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

S`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

_`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

D`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

D`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

X`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

X`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

|`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

D`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

X`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

#`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

#`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

V`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

E`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

S`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

+`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

;`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

q`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

{`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

X`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

}`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

S`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

X`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

+`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

;`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

j`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

I`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

5`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

5`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

J`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

P`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

X`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

_`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

_`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

X`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

_`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

{`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

}`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

X`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

z`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

D`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

E`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

+`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

E`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

+`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

^`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

+`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

;`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

7`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

;`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

S`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

C`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

I`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

I`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

W`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

7`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

#`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

#`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

W`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

j`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

I`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

5`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

q`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

'`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

D`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

X`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

_`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

4`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

#`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

#`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

W`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

D`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

3`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

3`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

j`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

S`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

W`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

3`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

4`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

5`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

4`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

4`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

8`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

8`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

3`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

3`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

8`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

3`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

;`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

W`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

;`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

B`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

B`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

'`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

8`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

8`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

'`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

B`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

_`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

'`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

W`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

G`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

4`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

5`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

8`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

3`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

4`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

4`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

B`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

_`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

'`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

7`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

9`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

3`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

z`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

S`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

j`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

j`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

L`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

W`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

j`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

3`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

4`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

4`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

4`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

8`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

8`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

W`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

'`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

X`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

j`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

3`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

9`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

3`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

D`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

_`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

S`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

_`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

j`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

;`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

3`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

9`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

4`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

M`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

_`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

[`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

5`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

_`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

_`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

9`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

7`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

I`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

_`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

4`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

_`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

D`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

4`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

5`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

_`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

I`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

5`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

#`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

#`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

D`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

S`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

P`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

z`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

I`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

F`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

E`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

8`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

8`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

5`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

3`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

j`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

3`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

5`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

5`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

5`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

3`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

4`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

W`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

;`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

H`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

S`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

5`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

/`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

5`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

3`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

I`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

D`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

"`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

"`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

^`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

~`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

4`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

N`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

L`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

j`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

A`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

B`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

F`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

S`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

<`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

=`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

``0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

6`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

F`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

T`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

7`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

1`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

0`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

"`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

"`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

j`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

L`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

j`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

x`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

D`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

*`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

z`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

:`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

B`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

F`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

"`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

"`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

q`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

—`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

2`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

"`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

"`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

m`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

'`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

X`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

O`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

R`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

p`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

,`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

"`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

k`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

y`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

"`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

(`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

)`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

W`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

h`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

-`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

v`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

u`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

b`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

f`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

e`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

c`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

o`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

r`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

d`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

n`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

g`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

i`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

t`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

s`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

 `0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

w`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

a`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

l`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.

.`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.


`0111` and `1000` resist this route even with 40 candidates at depth 15.
`1000` does build through the original direct scan, so the two routes are
complementary: **combined they generate 15 of the 16 two-input tables**
(`minifuck_combined.py`, ~5.5 minutes), leaving only `0111` (OR).

Two things about that last table are known.  The endgame's print orientation
is decided by cell 7 — with cell 7 at 0, pointer 6 prints `'1'`; with cell 7
at 1 it prints `'0'` — and `pool_fix` targets only cells 0..6, so the
orientation was being inherited rather than chosen.  Pinning cell 7 gives the
endgame both orientations (and does build `0110` that way), but it does not
reach `0111`: over every parked candidate and every accumulator cell, the
endgame's outputs cover 12 of the 16 possible tables, and the four it misses
are exactly `0110`, `0111`, `1000`, `1101`.  So the limit is what the
endgame's transform can land on from these candidates, not the orientation.
